
const Tile = ({value}) => {
    return(
        <div id={value} className={"flex items-center justify-center text-center border-2 border-outset"}>
            
        </div>
    )
}

export default Tile;